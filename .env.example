# ===== Hoy: necesarias para correr la base =====
GROQ_API_KEY=tu_api_key_de_groq
GROQ_MODEL=openai/gpt-oss-120b
PORT=3040

# ===== Objetivo: retos MLH (descomenta conforme se integren; nunca subas valores reales) =====
# LLM_PROVIDER=groq                    # groq | gemini  (fallback de orquestación)
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
ELEVENLABS_AGENT_ID=    # ID del agente de ElevenLabs ConvAI (Maya). Créalo en elevenlabs.io/app/conversational-ai
# BANK_DATA_SOURCE=memory             # memory | tiger (fuente de datos del servidor MCP)
# DATABASE_URL=postgres://user:pass@host:5432/norte   # Tiger Cloud (TimescaleDB)
# DATABASE_URL=postgres://norte:norte@timescaledb:5432/norte   # con docker compose local
# SNOWFLAKE_ACCOUNT=
# SNOWFLAKE_USER=
# SNOWFLAKE_PASSWORD=
# SNOWFLAKE_WAREHOUSE=
# SNOWFLAKE_DATABASE=
# PUBLIC_API_URL=http://localhost:3040   # URL que usa la app móvil / widget

# ===== Google Calendar MCP =====
# Credenciales del OAuth client (tipo "Desktop app") creado en
# https://console.cloud.google.com/auth/clients (proyecto con Google Calendar API habilitada)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:3045/oauth2callback
# Tras configurarlas, corre una vez: npm run google:auth
